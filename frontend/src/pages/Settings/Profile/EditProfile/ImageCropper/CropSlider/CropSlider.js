import React from "react";
import styles from "./CropSlider.module.css";

const CropSlider = () => {
  return (
    <input type="range" className={styles.rangeMin} onChange={(e) => {}} />
  );
};

export default CropSlider;
